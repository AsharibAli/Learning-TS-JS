const superHeros: string[] = []
// const heroPower: number[] = []
const heroPower: Array<number>=[] //Another Way

type User = {
    name: string;
    isActive: boolean;
}
const AllUsers: User[] = []

const MLModels: number[][] = [
    [255, 255, 255],
    []
]

superHeros.push("Spiderman");
heroPower.push(2);

AllUsers.push({ name: "", isActive: true })

export{}